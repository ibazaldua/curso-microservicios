spring:
  application:
    name: estado-cuenta-resotor

  cloud:
    config:
      uri: 
      - http://192.168.15.23:8888/
      - http://localhost:8888/
      
server:
  port: 8081
  
#eureka:
 #client:
  # service-url:
   #  defaultZone:  http://192.168.15.23:8761/eureka
#management.endpoints.web.exposure.include=hystrix.stream
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, info, health


     


